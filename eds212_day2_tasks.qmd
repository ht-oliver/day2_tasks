---
title: "eds212-day2-tasks"
author: "Henry Oliver"
format: html
---

# Find the slope of f(x) = x^2 at x = 3

### Create the function

```{r}
fx <- expression(x^2)
```


```{r}
df_dx <- D(expr = fx, name = "x")
df_dx
```

```{r}
x <- 3
eval(df_dx)
```

